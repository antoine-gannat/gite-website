import { makeStyles } from "@fluentui/react-make-styles";

export const useStyles = makeStyles({
  title: {
    textAlign: "center",
    marginTop: "20px",
  },
  separation: {
    width: "100%",
    minWidth: "400px",
    color: "black",
    borderWidth: "2px",
  },
});
